#include "main.h"

/**
 * read_textfile - function
 * @filename: filename
 * @letters: num of letters
 * Return: returns size
 */
ssize_t read_textfile(const char *filename, size letters)
{
	ssize_t sizeread;
	
	return (sizeread);
}
