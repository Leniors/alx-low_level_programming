#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char letter = 'a';

	while (letter <= 'z')
	{
		printf("%c", letter);
		letter++;
		if (letter == e || letter == q)
		{
			continue
		}
	return (0);
}
